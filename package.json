{
  "name": "discord-bot",
  "version": "0.2.0",
  "description": "My experiments with creating a Discord bot",
  "author": "Marcus Cemes",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "debug": "node --inspect-brk -r ts-node/register -r dotenv/config ./src/",
    "develop": "ts-node-dev -r dotenv/config ./src/",
    "start": "ts-node -r dotenv/config --files ./src/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@types/node": "^13.11.1",
    "@types/pino": "^5.17.0",
    "axios": "^0.19.2",
    "discord.js": "^12.1.1",
    "pino": "^6.1.1"
  },
  "devDependencies": {
    "@types/natural": "^0.6.3",
    "dotenv": "^8.2.0",
    "pino-pretty": "^4.0.0",
    "ts-node": "^8.8.2",
    "ts-node-dev": "^1.0.0-pre.44",
    "typescript": "^3.8.3"
  }
}
